{
  "name": "swisswayexplorer",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sanity:check": "node --env-file=.env scripts/check-sanity.mjs",
    "deploy:webhook:check": "node --env-file=.env scripts/trigger-coolify-webhook.mjs",
    "deploy:webhook:trigger": "node --env-file=.env scripts/trigger-coolify-webhook.mjs --execute",
    "astro": "astro"
  },
  "dependencies": {
    "@sanity/client": "^7.20.0",
    "astro": "^6.1.3"
  },
  "devDependencies": {
    "@sanity/image-url": "^2.1.1",
    "sanity": "^5.19.0"
  }
}
